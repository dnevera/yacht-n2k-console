- type: grid
  cards:
    - type: heading
      heading: AIS
    - id: map
      type: map
      entities:
        - entity: device_tracker.nevera
      geo_location_sources:
        - ais_targets
      default_zoom: 12
      aspect_ratio: 16x9
    - id: detail_list
      type: markdown
      title: AIS Targets
      # `content` is generated by helpers/build.py from config.yaml's
      # `detail_fields` list (native Jinja template, evaluated by HA itself —
      # no extra HACS card needed, see README.md "Why no auto-entities").
      content: PLACEHOLDER
