services:
  local-ha:
    container_name: local-ha
    build:
      context: .
      dockerfile: Dockerfile
    image: local-ha:latest
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./config:/config
      - /etc/localtime:/etc/localtime:ro
    environment:
      - TZ=Europe/Moscow
