# <Краткое описание бага>

## Metadata

- id: NNN
- type: bugfix
- status: draft
- owner: <owner>
- date: YYYY-MM-DD

## Context

Симптом, как воспроизводится, когда появился, затронутые компоненты.

## Requirements

- Ожидаемое поведение после фикса.
- Что НЕ должно измениться (регрессии).

## Architecture & Technical Design

Корневая причина (root cause) и предлагаемое исправление. Почему именно так, а не иначе.

## Interfaces / Contracts

Изменения контрактов (если есть). Если контракты не меняются — явно указать «без изменений».

## Implementation Plan

1. Репро-тест (падает до фикса).
2. Исправление — файл(ы).
3. Проверка отсутствия регрессий.

## Verification

- Репро-тест: `tests/...`
- Прогон затронутых тестов.

## Known Issues

Остаточные риски, связанные баги, ссылки на `patches/`.
